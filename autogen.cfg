MEMORY {
    HDR:  start = $0000, size = $0006, file = %O, fill = no;
    MAIN: start = $4000, size = $8000, file = %O, fill = no;
}
SEGMENTS {
    HEADER: load = HDR,  type = ro;
    CODE:   load = MAIN, type = ro;
}
